{
  "buildCommand": "npm run build:vercel",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.1 --activate && pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
